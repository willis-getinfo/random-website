pipeline {
    agent { docker { image 'node:14-alpine' } }
    stages {
        stage('build') {
            steps {
                echo 'olaa'
                sh 'npm --version'
                echo 'travei'
            }
        }
    }
}